#pragma once

#include <stdlib.h>
#include <stdint.h>

// --- CUSTOM TYPES ---

typedef uint8_t u8;
typedef int8_t i8;
typedef uint16_t u16;
typedef uint32_t u32;
typedef uint64_t u64;
typedef size_t usize;
